{
  "tasks": {
    "test": "echo \"No tests available in this repository.\"",
    "build": "pip install -r requirements.txt",
    "launch": "pip install -r requirements.txt && python LinkedinTelegramPostScheduleV5.py"
  }
}